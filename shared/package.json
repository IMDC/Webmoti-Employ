{
  "name": "@webmoti-employ/shared",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "pnpm --prefix .. start",
    "build": "tsc -b --force",
    "typecheck": "pnpm run build",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "zod": "catalog:"
  }
}
