{
  "name": "@webmoti-employ/server",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "pnpm --prefix .. start",
    "dev": "wrangler dev",
    "dev:electron": "pnpm run dev",
    "test": "vitest run",
    "vitest:watch": "vitest",
    "deploy": "wrangler deploy --minify",
    "install:deploy": "pnpm install --filter @webmoti-employ/shared --filter @webmoti-employ/server",
    "cf-typegen": "wrangler types worker-configuration.d.ts --env-interface CloudflareBindings",
    "db:typegen": "kysely-codegen",
    "db:better-auth-gen": "pnpm dlx @better-auth/cli@latest generate --config ./better-auth.config.ts --output ./src/db/better-auth-schema.sql",
    "postinstall": "node scripts/postinstall.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "migrate:better-auth:local": "cross-env MIGRATION_TARGET=LOCAL pnpm dlx @better-auth/cli@latest migrate --config ./better-auth.config.ts && pnpm db:typegen",
    "migrate:better-auth:remote": "cross-env MIGRATION_TARGET=REMOTE pnpm dlx @better-auth/cli@latest migrate --config ./better-auth.config.ts && pnpm db:typegen",
    "migrate:local": "cross-env MIGRATION_TARGET=LOCAL kysely migrate latest && pnpm db:typegen",
    "migrate:remote": "cross-env MIGRATION_TARGET=REMOTE kysely migrate latest && pnpm db:typegen",
    "migrate:local:all": "pnpm migrate:local && pnpm migrate:better-auth:local",
    "migrate:remote:all": "pnpm migrate:remote && pnpm migrate:better-auth:remote",
    "create-migration": "node scripts/create-migration-file.js"
  },
  "dependencies": {
    "@ai-sdk/groq": "3.0.15",
    "@hono-rate-limiter/cloudflare": "^0.2.2",
    "@hono/zod-validator": "^0.7.6",
    "@speechmatics/auth": "^0.1.0",
    "@webmoti-employ/shared": "workspace:^",
    "@zoom/videosdk": "catalog:",
    "ai": "6.0.49",
    "better-auth": "catalog:",
    "hono": "4.11.5",
    "hono-rate-limiter": "^0.5.3",
    "jose": "^6.1.3",
    "kysely": "^0.28.10",
    "pg": "8.17.2",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.6",
    "@types/pg": "^8.16.0",
    "cross-env": "^10.1.0",
    "dotenv": "^17.2.3",
    "globals": "^17.1.0",
    "kysely-codegen": "^0.19.0",
    "kysely-ctl": "^0.20.0",
    "wrangler": "4.49.1"
  }
}
